# Rust build artifacts
/target

# Frontend (vendored source is committed; build output is not)
/node_modules
/frontend/dist

# Local state
/.direnv
/.env
*.swp
.DS_Store
# Local config (holds API keys — mirror of the C project's gitignored
# config.conf).
/config.toml
